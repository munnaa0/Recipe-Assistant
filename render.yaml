services:
  - type: web
    name: recipe-chat-assistant
    runtime: node
    buildCommand: npm install
    startCommand: node api/server.js
    env: node
    envVars:
      - key: NODE_ENV
        value: production
